# v1.2.3

This patch repairs command targeting, asynchronous movement handoffs and browser/desktop parity, with responsive and keyboard-accessible controls.

- Keep route and joystick commands scoped to selected devices; prevent delayed starts after release or Stop All and stale route completion/hydration updates.
- Restore the packaged desktop LAN browser API, validate HTTP/WS inputs and static paths, and drain accepted commands before shutdown.
- Validate ADB/runtime payloads, share bounded GPX parsing, preserve newest history entries and bound road-planner work and timeouts.
- Fix mobile control clipping, modal focus/Escape behavior, stale connection callbacks and the incorrect Resume state after Teleport. Use a working OpenStreetMap default while retaining saved provider choices.
- Make type checks, builds and Docker release failures block delivery; stop Portainer deployment after failed migration/build/stop operations.

Validation: 272 tests in 25 files, all three TypeScript configurations, Electron/Web builds, Linux directory packaging and browser desktop/mobile checks. Device operations were simulated; physical Android and Windows/macOS launch checks were not performed locally.

Downloads: Windows Setup/Portable EXE, Linux AppImage (requires system ADB), macOS universal DMG/ZIP. macOS builds remain unsigned and unnotarized.

[Detailed audit and screenshots](../audits/2026-09-12/repo-ui-review.md)
