# Android ADB GPS Spoofer v1.2.2

- Keep the spoofed position marker visible after teleport handoffs and while a route is paused or moving.
- Reject late asynchronous keep-alive snapshots from a previous mode before they can clear or rewind the map marker.
- Preserve the route position across WebSocket initialization and hydrate the selected device after reconnects or device changes.
- Stabilize Leaflet marker identity and bundle Leaflet CSS in both desktop and Web builds.

## Validation

143 automated tests passed, including desktop and standalone Web delayed keep-alive handoff cases. Electron and standalone Web production builds passed. No physical devices were connected during validation.
